---
aliases:
  myansible: 'eval $(ssh-agent) ; sudo -E ssh-add /home/syscnf/.ssh/id_rsa'

ansible_connection: local

functions:
  config_host: |-
    ansible-playbook main.yaml -b -e "host_override=$1"

ssh_host_configs:
  puppet:
    HostName: 'dist.nwave.noaa.gov'
  simp-test-silv:
    HostName: '140.172.80.9'

use_tmux: true
